export default function Home() {
  return (
    <main className="min-h-screen bg-slate-950 p-10 text-slate-100">
      <h1 className="text-2xl font-semibold tracking-tight">VARUNA</h1>
      <p className="mt-2 max-w-xl text-sm text-slate-400">
        Vessel Attribution via Reverse-drift &amp; Unified Nowcast Analytics. Phase 1
        skeleton — the console is built against the fixture set from hour 5.
      </p>
    </main>
  );
}
